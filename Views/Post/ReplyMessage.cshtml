@model CoachCue.ViewModels.MessageViewModel

@{
    ViewBag.Title = "NewMessage";
    Layout = "~/Views/Shared/_PostLayout.cshtml";
}

<div class="msg-header">
    <h2>
        <input type="hidden" id="pst-type" value="msg" />
        <i class="icon-comment"></i>
        <span style="margin-left: 8px;">New Comment</span>
    </h2>
</div>

@{Html.RenderPartial("_MessageCtrl", model: Model);}

<div class="reply-message">
    <div class="row">
        <div class="user-avatar height" style="background: url(/assets/img/avatar/@Model.ParentMessage.user.avatar.imageName) 50% 50% no-repeat;"></div>
        <div class="msg-text">
            <div class="reply-username">@Model.ParentMessage.user.fullName <span class="reply-time">@@@Model.ParentMessage.user.userName</span></div>
            <div class="reply-time">@Model.ParentMessage.dateCreated.ToLongDateString() @Model.ParentMessage.dateCreated.ToShortTimeString()</div>
            <div class="reply-text">@Html.Raw(Model.ParentMessage.messageText)</div>
        </div>
    </div>
</div>